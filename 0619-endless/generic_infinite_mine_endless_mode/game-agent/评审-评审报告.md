# generic_infinite_mine_endless_mode 代码评审报告

> 评审时间: 2026-06-19 22:45
> 评审对象: /Users/miracleshen/GameAgent/projects/generic_infinite_mine_endless_mode
> 对照产物: 需求-最终PRD / 架构-技术方案 / 设计-元素实现清单 / 验证-页面功能实现清单 / 需求-验收用例
> 评审 Skill: standalone-reviewer

## 评审结果: ✅ PASS

## 统计

| 指标 | 数量 |
|------|------|
| P0 问题 | 0 |
| P1 问题 | 0 |
| tech-spec 不一致 | 0 |
| 元素实现清单违规 | 0 |
| PRD 功能未覆盖 | 0 |
| 页面功能清单违规 | 0 |
| 验收用例未实现/演示直通 | 0 |
| warning | 0 |

---

## Layer 0: 构建时检查

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 0.1 TypeScript 类型检查（`tsc --noEmit`） | ✅ | 命令 `cd /Users/miracleshen/GameAgent/projects/generic_infinite_mine_endless_mode && npx tsc --noEmit` 零输出，零错误 |
| 0.2 Vite 构建（`vite build`） | ✅ | 构建成功：`✓ 89 modules transformed`，`dist/index.html 0.44 kB`，`dist/assets/index-DdrE6f0F.js 223.76 kB / gzip 70.31 kB`，`✓ built in 545ms`；`dist/index.html` 与 `dist/assets/` 存在 |
| 0.3 Playwright 运行时 | ✅ | runtime-check status=`ok`；`mine_home`、`endless_mine`、`modal_confirm_return`、`modal_result`、`modal_blocker` 均无 pageerror / 非噪声 4xx |

### P0 问题（构建/类型错误）

| # | 来源 | 文件 | 行号 | 错误信息 | 修复建议 |
|---|------|------|------|---------|---------|
| — | — | — | — | 无 | — |

### P1 问题

| # | 来源 | 信息 | 说明 |
|---|------|------|------|
| — | — | 无 | — |

---

## Layer 1: 确定性检查

### 1.1 ES 兼容性红线（P0）

| # | 文件 | 行号 | 命中模式 | 代码片段 | 修复建议 |
|---|------|------|---------|---------|---------|
| — | — | — | 无 `.flat()` / `.flatMap()` / `.at()` / `.replaceAll()` / `Object.fromEntries` / `Array.from({ length })` 命中 | — |

### 1.2 编码规范（P1）

| # | 文件 | 行号 | 命中规则 | 代码片段 | 修复建议 |
|---|------|------|---------|---------|---------|
| — | — | — | 无未守卫 `console.log`；防重锁使用 `useRef`；定时器均有 `clearTimeout` 清理；未发现 JSON.stringify 依赖；`key={index}` 仅用于固定 42 格 hit grid，不涉及增删列表 | — | — |

### 1.3 样式规范（standalone 专属）

| # | 检查项 | 状态 | 说明 |
|---|-------|------|------|
| 1.3.1a | `src/styles/global.less` 存在 | ✅ | 文件存在 |
| 1.3.1b | `global.less` 包含 `@import './variables.less'` | ✅ | 第 1 行为 `@import './variables.less';` |
| 1.3.1c | `src/styles/variables.less` 存在 | ✅ | 文件存在 |
| 1.3.1d | `variables.less` `:root` 块非空（≥ 1 个 CSS Variable） | ✅ | `:root` 含 10 个 CSS Variable：`--color-primary`、`--color-secondary`、`--color-bg`、`--color-text`、`--color-text-light`、`--color-panel`、`--color-accent-blue`、`--font-size-sm`、`--font-size-md`、`--font-size-lg` |
| 1.3.1e | `main.tsx` 导入 `./styles/global.less` | ✅ | `src/main.tsx:12` 导入 `./styles/global.less` |
| 1.3.2 | 组件级 less 全部 `.750.less` 后缀 | ✅ | `find src/components src/pages -name '*.less' ! -name '*.750.less'` 无输出 |
| 1.3.3 | 无覆盖 root font-size / 无 JS 动态根字号 / 未启用 pxtorem | ✅ | 命中仅为注释或 CSS 变量名；未发现 `html { font-size }`、`document.documentElement.style.fontSize`、`pxtorem(` 调用 |
| 1.6.1 | 无非法整页缩放 | ✅ | 命中仅为注释；实际缩放使用 `scale(var(--scene-scale, 1))` |
| 1.6.2 | 缩放管线完整 | ✅ | `src/utils/index.ts:66-67` 设置 `--scene-scale` 与 `--scene-vh`；`src/styles/page-common.less:35-36` 消费 `var(--scene-vh)` 与 `scale(var(--scene-scale, 1))` |
| 1.6.3 | 弹窗 / 浮层居中方式 | ✅ | `src/styles/page-common.less:49-56` 使用 `translate(-50%, -50%) scale(var(--scene-scale, 1))`；未用 flex 居中 750px 弹窗画布 |
| 1.6.4 | 底部锚 | ✅ | `BottomAction`、`BottomDock` 使用 `bottom: calc(... + env(safe-area-inset-bottom))` |

### 1.4 素材路径

| # | 文件 | 行号 | 问题 | 修复建议 |
|---|------|------|------|---------|
| — | — | — | JS/TSX 图片 `src` 均为 `./assets/imgs/...` 或 PageShell 动态拼接；Less `url()` 均为 `/assets/imgs/...`；无 `public` import | — |

### 1.5 Emoji 占位残留

| # | 文件 | 行号 | 匹配字符 | 建议替换的素材（查清单） |
|---|------|------|---------|----------------------|
| — | — | — | 无代码非注释位置 emoji；仅 PRD/注释文案提及工具格符号，不构成代码占位 | — |

---

## Layer 2: 语义级检查

### 2.1 tech-spec 一致性

| # | 检查项 | 状态 | 差异说明 |
|---|-------|------|---------|
| 2.1.1 | 页面组件齐全 | ✅ | `MineHomePage`、`EndlessMinePage` 存在；弹窗由 `src/components/modals/index.tsx` 实现 `ConfirmReturn` / `Result` / `Blocker` |
| 2.1.2 | Store slice 齐全 | ✅ | `src/store/slices/appSlice.ts`、`src/store/slices/mineSlice.ts` 存在 |
| 2.1.3 | Store 字段一致 | ✅ | app slice 含 route/loading/homeEnergy/homeCoin/premiumCurrency/activeModal/modalPayload/toastQueue/entryLocked/mainOreBag/mainMaterialBag；mine slice 含 runId/runState/depth/depthStageId/stageProgressText/board/nextRowPreview/tools/selectedTool/previewCells/supplyValue/supplyMax/harvest/unlockedFlags/lastInvalidReason/settlement |
| 2.1.4 | API 函数齐全 | ✅ | `getMineHome`、`submitEnterEndlessMine`、`getEndlessMineConfig`、`submitDigAction`、`submitSettleMine`、`getPendingMineRun` 均在 `src/apis/index.ts` 导出 |
| 2.1.5 | Mock 覆盖 | ✅ | `src/apis/mocks/index.ts` 覆盖上述 6 个 API 的 mock 实现 |
| 2.1.6 | Hook 齐全 | ✅ | `src/hooks/useMineHome.ts`、`src/hooks/useEndlessMine.ts` 存在，核心输出与页面使用一致 |
| 2.1.7 | 状态枚举齐全 | ✅ | `src/constants/enum.ts` 覆盖页面、RunState、ToolType、CellType、DepthStageId、ModalType |
| 2.1.8 | 技术栈 fixed-stage/px 一致性 | ✅ | `架构-技术方案.md:17` 明确 `Less + fixed-stage/px`、`index.750.less`、禁止 rem/pxtorem；实现使用 `src/styles/page-common.less` + `src/main.tsx` 注入 `--scene-scale` / `--scene-vh`，与实现一致 |

### 2.2 元素实现清单一致性（SSOT 核心）

| # | 检查项 | 状态 | 违规数 | 说明 |
|---|-------|------|--------|------|
| 2.2.1 | 文件名 SSOT（代码引用 ⊆ 清单建议文件名） | ✅ | 0 | 代码图片 stem 共 50 个，均出自清单建议文件名或 `ore_set` 子素材 key；无清单外凭空造图 |
| 2.2.2 | 渲染完整性（清单图片元素 ⊆ 代码引用） | ✅ | 0 | 背景经 `PageShell` 动态拼接渲染；Pixi 纹理通过 `AssetLoader` / `assetForCell` 渲染；共享与弹窗素材均有引用 |
| 2.2.3 | 实现方式锁定（图片元素未被 CSS 降级） | ✅ | 0 | 图片元素使用 `<img>`、CSS `url('/assets/imgs/...')` 或 Pixi texture；未发现图片元素被纯 CSS 替代 |
| 2.2.4 | 实现方式锁定（CSS 元素未引用图片） | ✅ | 0 | CSS-only 槽位、文本、遮罩、预览层以 CSS 背景/边框/文字实现；未错误引用图片 |
| 2.2.5 | 几何锁定 + 坐标空间 | ✅ | 0 | 关键几何与清单一致：TopHud、RunHarvest、SupplyBar、PixiMineCanvas、ToolBar、BottomAction、弹窗位置均按 750px 设计坐标；嵌套元素使用 parent-local 坐标；底部按钮使用 bottom 安全区锚 |
| 2.2.6 | 父子关系一致 | ✅ | 0 | JSX 嵌套与清单父子关系一致；弹窗用 OverlayStage 作为浮层画布 |
| 2.2.7 | 无字底板文字叠加 + 重叠守门 | ✅ | 0 | 无字底板如入口卡、按钮、弹窗底板均叠加代码文本；带字整图元素未重复叠字 |

#### 违规明细

| # | 元素ID | 违规类型 | 清单定义 | 代码实际 | 修复建议 |
|---|--------|---------|---------|---------|---------|
| — | — | — | — | 无违规 | — |

### 2.3 PRD 功能覆盖

| # | 功能 | 状态 | 对应代码 |
|---|------|------|---------|
| F001-F004 | 入口、防重、体力扣减、20 木镐开局 | ✅ | `src/hooks/useMineHome.ts:34-54`；`src/apis/mocks/index.ts:27-41` |
| F005-F012 | 顶部 HUD、状态区、收获区、矿井主场景、7×6、深度线、真实下一行、补给条 | ✅ | `src/pages/EndlessMinePage/index.tsx:28-35`；`src/components/*`；`src/game/EndlessMineGame.ts:250-270` |
| F013-F022 | 三类工具、选中高亮、木镐/炸弹、预览、无效反馈 | ✅ | `src/components/ToolBar/index.tsx`；`src/game/EndlessMineGame.ts:54-93,122-181`；`src/game/BoardModel.ts:19-33` |
| F023-F031 | 七类格子/内容、宝箱、工具格、掉落与工具补给 | ✅ | `src/game/BoardGenerator.ts:22-75`；`src/data/cellConfigs.ts:3-24` |
| F032-F037 | 层数积分、掉落积分、30% 随机积分、积分满补给、门槛和奖励随深度提升 | ✅ | `src/game/BoardGenerator.ts:95-100`；`src/game/EndlessMineGame.ts:195-205`；`src/data/depthStages.ts:4-10` |
| F038-F050 | 持续生成、五深度段、解锁阈值、概率提升、阶段反馈、通路保证 | ✅ | `src/game/BoardGenerator.ts:13-31,78-93,102-107`；`src/game/EndlessMineGame.ts:207-219,272-294` |
| F051-F059 | 主动退出、返回确认、工具耗尽、收获弹窗、收益回流、背包满/断线/未结算恢复口径 | ✅ | `src/hooks/useEndlessMine.ts:105-130`；`src/components/modals/index.tsx:18-67`；`src/apis/mocks/index.ts:76-104`；`src/hooks/useMineHome.ts:21-27` |
| F060-F068 | 挖掘反馈、爆炸反馈、资源飞入、数值跳动、阶段庆祝、高价值反馈、下陷/上浮、移动端单屏 | ✅ | `src/game/EffectLayer.ts`；`src/components/RunHarvest/index.tsx:12-16`；`src/pages/EndlessMinePage/index.tsx:36-43`；`src/styles/page-common.less` |
| F069 | P0 不包含项未作为核心系统新增 | ✅ | 未发现局内商店、特殊事件层、路线分支层、多失败惩罚、独立成就体系实现 |

#### 状态机完整性

| # | PRD 状态 | 枚举定义 | 路由分支 | Reducer 覆盖 |
|---|---------|---------|---------|-------------|
| 1 | mine_home_available / mine_home_energy_insufficient / entering_mine | ✅ | ✅ MineHomePage | ✅ appSlice loading/entryLocked + useMineHome |
| 2 | progressing / tool_selected / bomb_preview / dig_resolving / stage_up_prompt / rare_unlock_toast | ✅ | ✅ EndlessMinePage | ✅ mineSlice + EndlessMineGame callbacks |
| 3 | return_confirm / manual_exit_requested / tools_depleted / settling / result_popup / returned_to_mine_home / interrupted | ✅ | ✅ ModalLayer + route reset | ✅ useEndlessMine + app/mine reducers |

### 2.4 页面功能实现清单复验

| # | 检查项 | 状态 | 违规数 | 说明 |
|---|-------|------|--------|------|
| 2.4.1 | `验证-页面功能实现清单.md` 存在 | ✅ | 0 | 文件存在 |
| 2.4.2 | 页面章节覆盖 | ✅ | 0 | MineHomePage / EndlessMinePage / ModalConfirmReturn / ModalResult / ModalBlocker 均覆盖 |
| 2.4.3 | PRD 功能进入清单 | ✅ | 0 | H1-H5、M1-M12、CR1、R1、B1 覆盖 PRD 关键功能 |
| 2.4.4 | 页面手册交互进入清单 | ✅ | 0 | 入口、挖掘、工具、炸弹、结算、弹窗、toast、布局均进入清单 |
| 2.4.5 | 实现位置有效 | ✅ | 0 | 清单列出的文件均存在，关键 handler / class / API 能在代码中定位 |
| 2.4.6 | 按类型复验关键逻辑 | ✅ | 0 | static/state/api/modal/navigation/animation 均有代码证据 |
| 2.4.7 | 可观察结果有效 | ✅ | 0 | 清单可观察结果均对应 UI、状态字段、弹窗、动画或数据更新路径 |
| 2.4.8 | 自检状态无未处理项 | ✅ | 0 | 文件内自检列均为 ✅；无 ❌ / ⚠️ |

#### 页面功能违规明细

| # | 页面 | 功能点 | 清单状态 | 代码证据 | 复验结果 | 问题 |
|---|------|--------|----------|----------|----------|------|
| — | — | — | — | — | ✅ 无违规 | — |

### 2.5 功能验收用例逐条判定（防演示直通）

| # | 用例ID | 断言（期望可观察变化） | 代码证据 | 判定 | 问题 |
|---|--------|----------------------|---------|------|------|
| 1 | AC-F001-F004 / AC-N001-N002 / AC-S002 | 防重、最多扣 1 体力、体力不足不入场、入场发 20 木镐 | `useMineHome.ts:34-54`; `mocks/index.ts:27-41`; `mineSlice.ts:50-65` | ✅ 实现 | — |
| 2 | AC-F005-F012 | HUD、状态、收获、7×6、深度线、真实下一行、补给条可见 | `EndlessMinePage/index.tsx:28-35`; `PixiMineCanvas/index.tsx:26-46`; `SupplyBar`; `StatusInfo`; `RunHarvest` | ✅ 实现 | — |
| 3 | AC-F013-F022 / AC-N003-N005 / AC-S003-S005 | 三工具可见可选，木镐/炸弹消耗与命中规则、不可敲与不可释放不结算 | `EndlessMineGame.ts:54-93,122-181`; `BoardModel.ts:19-33`; `ToolBar` | ✅ 实现 | — |
| 4 | AC-F023-F031 / AC-N006 | 格子类型、耐久、宝箱/工具格自动收益、沙砖不直接给基础金币材料 | `BoardGenerator.ts:22-75`; `cellConfigs.ts:3-12` | ✅ 实现 | — |
| 5 | AC-F032-F037 | 深度积分、掉落积分、30% 随机积分进入 bar、积分满工具补给、门槛/奖励随深度提升 | `BoardGenerator.ts:95-100`; `EndlessMineGame.ts:195-205`; `depthStages.ts:4-10` | ✅ 实现 | — |
| 6 | AC-F038-F050 / AC-N007-N008 / AC-S006-S007 | 持续生成、五阶段、解锁阈值、概率提升、阶段提示、通路保证 | `BoardGenerator.ts:13-31,78-93,102-107`; `EndlessMineGame.ts:207-219,272-294` | ✅ 实现 | — |
| 7 | AC-F051-F055 / AC-N009-N010 / AC-S008-S010 | 收队返场、返回确认、工具耗尽结算、收获弹窗与返回主页 | `useEndlessMine.ts:105-127`; `modals/index.tsx:18-67` | ✅ 实现 | — |
| 8 | AC-F056-F059 / AC-N011 | 金币/矿品/材料结算回流、背包满按主系统、断线/未结算恢复 | `useEndlessMine.ts:111-114`; `appSlice.ts:40-47`; `mocks/index.ts:76-104`; `useMineHome.ts:21-27` | ✅ 实现 | — |
| 9 | AC-F060-F065 | 挖掘/爆炸/飞入/跳动/阶段庆祝/高价值反馈 | `EffectLayer.ts:41-93,119-160`; `EndlessMineGame.ts:168-193`; `RunHarvest.tsx:12-16`; `EndlessMinePage/index.tsx:36-43` | ✅ 实现 | — |
| 10 | AC-F066-F069 / AC-N012 | 下陷/上浮表现、移动端单屏、未新增 P0 不包含系统 | `AssetLoader.ts:1-6`; `EndlessMineGame.ts:296-304`; `page-common.less:30-56`; component `.750.less` | ✅ 实现 | — |

### 2.6 素材集完整性对账

| 组前缀 | PRD期望N | 清单声明N | 产出/代码引用数 | 判定 | 问题 |
|--------|---------:|-----------:|---------------:|------|------|
| ore_set | 9 | 9 | 9 个子素材 key 在代码/纹理预加载中可引用 | ✅ | — |

---

## 重点复核结论

| 重点项 | 结论 | 证据 |
|---|---|---|
| AC-F034：当前深度类型最大层数 30% 随机积分奖励是否进入补给积分条 | ✅ PASS | `BoardGenerator.ts:95-100` 计算 `Math.floor(stageMaxDepth * 0.3)` 并写入 payload `supplyScore`；`EndlessMineGame.ts:195-205` 将 `supplyGain` 累加到 `this.supplyValue`，积分满后发工具补给 |
| AC-F056：矿品/材料是否回流主系统背包/材料背包 | ✅ PASS | `mockSubmitSettleMine` 将 `settlement.harvest.ores` 合并进 `mainOreBag`、`materials` 合并进 `mainMaterialBag`；`appSlice.applyMainSettlement` 同步写入 Redux 主系统资源 |
| AC-F062：资源飞入是否按资源类型飞到顶部累计区对应槽位，不再 dummy rect / 写死目标坐标 | ✅ PASS | `RunHarvest` 为 coin/ore/material 三槽分别提供 ref；`useEndlessMine.ts:33-40` 的 `getResourceTargetRect(type)` 按资源类型返回对应 DOMRect；`EndlessMineGame.ts:184-193` 仅在对应资源 delta 存在时调用 `showResourceFly(type, from, targetRect)`；`EffectLayer.ts:60-68` 使用传入 DOMRect 计算目标中心，未见 dummy rect 或写死目标坐标 |
| `架构-技术方案.md` 技术栈是否已与 fixed-stage/px 实现一致 | ✅ PASS | 技术方案 `Less + fixed-stage/px`；实现 `page-common.less`/`main.tsx` 使用 fixed-stage 设计 px、`--scene-scale`、`--scene-vh`，未启用 rem/pxtorem |
| P0/P1/warning 是否为 0 | ✅ PASS | 本次 Layer 0/1/2 统计 P0=0、P1=0、warning=0 |

---

## 系统性问题标记

无。本次未发现 3 次以上重复同类问题。

---

## 评审结论

**判定规则：**

| 结果 | 条件 |
|------|------|
| ✅ PASS | 所有层 P0 = 0 且 PRD 功能未覆盖 = 0 且页面功能清单违规 = 0 且验收用例未实现/演示直通 = 0 |
| ⚠️ PASS WITH WARNINGS | 所有层 P0 = 0 但有 P1 或 tech-spec 不一致 |
| ❌ NEEDS FIX | 任意层 P0 > 0 或 PRD 功能未覆盖 > 0 或页面功能清单违规 > 0 或验收用例未实现/演示直通 > 0 |

**本次结论：✅ PASS**

P0 问题摘要：无。

P1 问题摘要：无。

warning 摘要：无。
