# kg_ai_anchor_sticker_pack 代码评审报告

> 评审时间: 2026-06-04 20:16
> 评审对象: /Users/xinyi/work/projects-cnb/pages/projects/kg_ai_anchor_sticker_pack
> 对照产物: 需求-最终PRD / 架构-技术方案 / 设计-元素实现清单 / 验证-字段实现矩阵
> 评审 Skill: tapd-reviewer

## 评审结果: ⚠️ PASS WITH WARNINGS

## 统计

| 指标 | 数量 |
|------|------|
| Layer 0 P0（构建/类型） | 0 |
| Layer 1 P0（代码红线） | 0 |
| Layer 1 P1（规范/运行时） | 0 |
| 技术方案 不一致 | 1 |
| 元素清单 不一致 | 0 |
| PRD 功能 P0（说明/文案/触发/分支/流转） | 0 |
| PRD 功能 P1（异常/配置） | 3 |
| 素材缺失（需人工补素材，不计入判定） | 0 |

---

## Layer 0: 构建时检查

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 0.1 TypeScript 类型检查（`tsc --noEmit`） | ✅ | 针对 `projects/kg_ai_anchor_sticker_pack/` 过滤后零错误。 |
| 0.2 tme-build 构建 | ✅ | 构建成功，输出 `js/index.web.f2843927.js`、`index.web.50f813f7.css`；仅有已知无害 `DefinePlugin` warning。 |
| 0.3 ESLint 扫描 | ✅ | eslint 可用（v7.32.0），项目 TS/TSX 扫描无错误输出。 |
| 0.4 运行时检查（Playwright） | 跳过 | 未执行（原因：`playwright not found`，环境不可用）。 |

### P0 问题（构建/类型错误）
| # | 来源 | 文件 | 行号 | 错误信息 | 修复建议 |
|---|------|------|------|---------|----------|
| — | — | — | — | 无 | — |

---

## Layer 1: 确定性检查

> 规则来源：tapd-code-development/references/grep-rules.md。已按 grep-rules.md 扫描项目 TS/TSX/Less/tpl 范围；`activityId/activityID` 命中经读取上下文确认是 URL/PAGE_INFO 读取，不属于硬编码活动 ID；`setTimeout/setInterval` 命中经读取上下文确认同文件存在 cleanup；`reportProgress(100)` 位于页面数据加载完成处，符合 1.7 规则。

### P0 问题（必须修复）
| # | grep-rules 分组 | 文件 | 行号 | 命中模式 | 代码片段 | 修复建议 |
|---|------|------|------|---------|---------|---------|
| — | — | — | — | — | 无 | — |

### P1 问题（建议修复）
| # | grep-rules 分组 | 文件 | 行号 | 命中模式 | 代码片段 | 修复建议 |
|---|------|------|------|---------|---------|---------|
| — | — | — | — | — | 无 | — |

---

## Layer 2: 语义级检查

### 2.1 技术方案一致性
| # | 检查项 | 状态 | 说明 |
|---|--------|------|------|
| 2.1.1 | 路由/页面组件齐全 | ✅ | 技术方案要求 `AuthorizationPage`；实际 `routes.ts:3-12` 指向 `pages/AuthorizationPage`。 |
| 2.1.2 | 目录约定 | ❌ | 技术方案 §3 要求 `src/pages` / `src/components` / `src/store` / `src/apis` / `src/constants`，实际主要目录位于项目根 `pages` / `components` / `store` / `apis` / `constants`，仅存在孤立 `src/constants/images.ts`。这是方案与工程结构不一致，按 warning 记录。 |
| 2.1.3 | API 函数齐全 | ✅ | 技术方案 §4 要求 `queryActivityStatus` / `submitAuth`；实际 `apis/index.ts:31`、`apis/index.ts:53` 均已导出。 |
| 2.1.4 | JCE 类型 re-export（无手写 $XxxRsp） | ✅ | `types/index.ts:1-10` 从 `jce/kg/aigc_sticker_gift/proto_aigc_sticker_gift_webapp` re-export 方案要求的 Req/Rsp 类型；未发现手写等价 Req/Rsp interface。 |
| 2.1.5 | Store 字段匹配 | ✅ | `store/reducer/main.ts:45-59` 覆盖方案 §5 字段，并包含私有状态 `isInited`、`submitLocked`、`giftPanelOpening`、时间戳。 |
| 2.1.6 | Mock 覆盖 | ✅ | `apis/mocks/query_activity_status.json`、`apis/mocks/submit_auth.json` 存在；`apis/index.ts:31-63` 有 mock 分支。 |
| 2.1.7 | 动效落地（fx 参数一致） | ✅ | FX01 在 `pages/AuthorizationPage/index.tsx:103-108` + `pages/AuthorizationPage/index.750.less:65-67`；FX02 在 `pages/AuthorizationPage/index.tsx:174-180` + `pages/AuthorizationPage/index.750.less:288-292`，参数与方案一致。 |

### 2.2 元素实现清单一致性（六维度）
| # | 检查项 | 状态 | 违规数 | 说明 |
|---|--------|------|--------|------|
| 2.2.1 | img 用 IMAGES.{} 引用 | ✅ | 0 | 清单 img 列 `img_sticker_preview`、`card_anchor_avatar` 均在 `constants/images.ts:10-13` 定义，并在 `pages/AuthorizationPage/index.tsx:37`、`pages/AuthorizationPage/index.tsx:106`、`pages/AuthorizationPage/index.tsx:122-123`、`pages/AuthorizationPage/index.tsx:144` 引用。 |
| 2.2.2 | api 引入并 Hook 调用 | ✅ | 0 | `queryActivityStatus` / `submitAuth` 从 `../apis` 引入并在 `hooks/useAuthorizationPage.ts:179`、`hooks/useAuthorizationPage.ts:332` 调用。 |
| 2.2.3 | gamesdk 调用对应方法 | ✅ | 0 | 元素清单 gamesdk 列均为 `—`。 |
| 2.2.4 | component 实现方式一致 | ✅ | 0 | E05/E06 使用 `AdvancedImage`：`pages/AuthorizationPage/index.tsx:2`、`pages/AuthorizationPage/index.tsx:103-108`、`pages/AuthorizationPage/index.tsx:120-125`；E07 使用 `Tips`：`hooks/useAuthorizationPage.ts:4`、`hooks/useAuthorizationPage.ts:14-18`。 |
| 2.2.5 | skill 已实现或留 TODO | ✅ | 0 | E08 `gamesdk-gift-kg:gift-send-panel` 由 `hooks/useAuthorizationPage.ts:287-307` 承接，生成完成后调用客户端礼物面板并定位 tab/giftId。 |
| 2.2.6 | fx 动效实现 | ✅ | 0 | E05/FX01、E08/FX02 均有 CSS 动效实现。 |
| 2.2.7 | 优先级合规（skill≠— 则 gamesdk/component=—） | ✅ | 0 | E08 skill 非空且 gamesdk/component 均为 `—`。 |
| 2.2.8 | §4 SDK 全部有承接 | ✅ | 0 | 技术方案 §4 未列独立 GameSDK 方法；礼物面板由元素清单 skill 承接。 |

#### 违规明细
| # | 元素ID | 维度 | 清单定义 | 代码实际 | 文件:行号 | 修复建议 |
|---|--------|------|---------|---------|---------|---------|
| — | — | — | — | 无 | — | — |

### 2.3 PRD 功能覆盖

> 按 F### × 7 维度展开，仅列不一致项；某 F### 7 维度全过则不出现。

| # | F### | 维度 | 严重级 | PRD 原文 | 代码实际 | 文件:行号 |
|---|------|------|--------|---------|---------|---------|
| 1 | F003 | 配置项 / 后台数据依赖 | P1 | `支持主播上传 1-4 张照片；支持主播重新更换照片。` / `主播可上传 1-4 张照片作为 AI 生产图片来源` | 前端允许选择最多 4 张并保存到 `uploadedPhotos`，但提交时只取 `state.uploadedPhotos[0]` 传给 `submitAuth(imageUrl)`，未承接多张照片共同作为生产图片来源的语义；矩阵也将该点列为待确认。 | `hooks/useAuthorizationPage.ts:227-241`、`hooks/useAuthorizationPage.ts:324-327`、`apis/index.ts:53-71` |
| 2 | F004 | 异常处理 | P1 | `切换到上传照片但暂无可用上传照片时，阻断切换，不响应交互。` | 当前 `selectUploadedPhoto` 在无上传照片时调用 `openPhotoPicker()`，会拉起文件选择器，并非“阻断切换，不响应交互”。 | `hooks/useAuthorizationPage.ts:262-266`、`pages/AuthorizationPage/index.tsx:132-138` |
| 3 | F009 | 配置项 | P1 | `授权页面展示可配置的主题文案图片、页面预览图片、页面皮肤颜色、挂件背景及点击按钮颜色`；`支持配置的主题文案图片` | 页面预览图、皮肤颜色、挂件背景、按钮样式已承接；但主题文案图片未从后台配置读取，页面顶部使用硬编码文案 `主播定制表情包` / `LV.X及以上主播可定制`，未体现“主题文案图片”配置。 | `pages/AuthorizationPage/index.tsx:93-96`、`hooks/useAuthorizationPage.ts:123-148` |

#### PRD F### 维度覆盖摘要
| F### | 说明 | 文案 | 触发 | 分支 | 异常 | 配置 | 流转 |
|------|------|------|------|------|------|------|------|
| F001 | ✅ | ✅ | — | — | — | — | — |
| F002 | ✅ | — | — | ✅ | — | — | — |
| F003 | ✅ | — | ✅ | ✅ | ✅ | ❌ | — |
| F004 | ✅ | — | ✅ | ✅ | ❌ | — | — |
| F005 | ✅ | ✅ | — | ✅ | ✅ | — | — |
| F006 | ✅ | ✅ | ✅ | ✅ | ✅ | — | ✅ |
| F007 | ✅ | ✅ | ✅ | ✅ | ✅ | — | ✅ |
| F008 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| F009 | ✅ | — | — | ✅ | — | ❌ | — |
| F011 | ✅ | ✅ | — | ✅ | — | — | — |

> `—` 表示 PRD 该字段为空（无需检查）；`✅` 通过；`❌` 不一致已列入上表。

---

## 素材缺失（需人工补素材 · 不阻塞 · 不计入修复回路）

> 仅收录「代码接线正确（IMAGES.{key} 已定义+已引用+元素清单 img 列存在）但物理素材文件运行时 404」的元素。AI 被禁止创建图片文件，此类只能人工补素材，**不计入 P0/P1、不改变评审结论、不交 developer 返工**。判定规则见 layer0-build-checks.md §「素材 404 处置」。

无。

补充说明：Playwright 环境不可用，未捕获运行时 404；静态检查发现 `constants/images.ts:10-13` 中 `IMAGES.img_sticker_preview` 与 `IMAGES.card_anchor_avatar` 仍为空占位，需人工填入实际素材地址后重审，但本次不按“素材 404”计数。

---

## 系统性问题标记

无 3 次以上重复问题模式。

---

## 评审结论

| 结果 | 条件 |
|------|------|
| ✅ PASS | 所有层 P0 = 0（含 PRD 功能 P0） |
| ⚠️ PASS WITH WARNINGS | 所有 P0 = 0 但有 P1 / 技术方案不一致 / 元素清单不一致 / PRD 功能 P1 |
| ❌ NEEDS FIX | 任意 P0 > 0（Layer 0 构建/类型、Layer 1 代码红线、Layer 2.3 PRD 功能 P0 同权重） |

> 「素材缺失（需人工补素材）」不参与上表判定：即使存在素材缺失，只要 P0 = 0 仍可为 PASS / PASS WITH WARNINGS。

**本次结论：⚠️ PASS WITH WARNINGS**

必须修复的 P0 清单：无。

建议修复 / 澄清的 warning 清单：
1. 技术方案 §3 目录约定与当前工程根级目录结构不一致：应统一方案或工程结构。
2. F003 多图上传语义与 `submitAuth` 单图提交不一致：需确认后台是否支持多图或前端是否应仅选择单图。
3. F004 无上传照片时当前拉起选择器，不符合 PRD“阻断切换，不响应交互”。
4. F009 主题文案图片配置未承接，顶部文案仍硬编码。
