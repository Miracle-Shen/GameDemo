# 页面功能实现清单

> 阶段: Code Development
> 判定标准: 每个页面相关功能均有实现证据；未通过项同步写入 评审-coding反馈.md

## TitlePage / title

| # | 功能点 | 来源 | 类型 | 触发/展示元素 | 实现位置 | 关键逻辑 | 可观察结果 | 自检 |
|---|---|---|---|---|---|---|---|---|
| T1 | 标题页场景入口 | PRD §3.1/§7 | navigation | 进入无尽矿井按钮 | `src/pages/TitlePage/index.tsx` | `useAppActions.startNewRun` 调用 `submitStartRun` 后 `setRoute(endless_run)` | 点击按钮进入 RunPage | ✅ |
| T2 | 开始一局初始化 | PRD §3.2 | api/state | 入口按钮 | `src/hooks/useAppActions.ts` | API Mock 扣体力并 `resetRun` 初始化深度、工具、棋盘 | 新局木镐 20、横炸 2、九炸 2、深度 1m | ✅ |
| T3 | 图片 SSOT 渲染 | 元素清单 T01/T03 | static | 背景、入口按钮 | `src/pages/TitlePage/index.750.less` | 背景用 `bg_title.png`，按钮 bbox `[125,1015,503,185]` | 竖屏标题页单屏显示 | ✅ |

## RunPage / endless_run

| # | 功能点 | 来源 | 类型 | 触发/展示元素 | 实现位置 | 关键逻辑 | 可观察结果 | 自检 |
|---|---|---|---|---|---|---|---|---|
| R1 | 7×6 棋盘展示 | PRD §3.3/§3.6 | static | Canvas 2D 棋盘视口 | `src/pages/RunPage/index.tsx`, `src/game/MineBoardRenderer.ts` | 棋盘容器按竖屏移动端安全边距扩大为 `[28,300,694,642]`，Canvas 100% 填充容器，Canvas 2D 预加载 9 个 tiles/marker 子素材并渲染 board snapshot | 中部挖矿区域更大且完整适配移动端单屏 | ✅ |
| R2 | 木镐/横炸/九炸工具 | PRD §3.4 | interactive/state | 底部工具栏 | `src/game/EndlessMineGame.ts`, `src/game/MineBoardModel.ts` | 选中工具后校验库存；木镐可点击坑道边缘可破坏格，横炸/九炸落点必须是已连通 EMPTY 坑道格；合法点击消耗对应工具 | 工具数量动态变化，非法炸弹落点不消耗库存 | ✅ |
| R3 | WALL 不可破坏与连通限制 | PRD §3.4 | interactive | 棋盘点击 | `src/game/MineBoardModel.ts` | `isLegalTarget` 校验 WALL、坑道邻接；非法 toast 且不消耗工具 | 非法点击出现提示 | ✅ |
| R4 | 遮挡层先破坏 | PRD §3.3/§3.4 | state | 棋盘格 | `src/game/MineBoardModel.ts` | `coverVisible` 优先置 false，第二次才伤害真实内容 | 遮挡层被翻开后显示内容格 | ✅ |
| R5 | 深度推进与新行生成 | PRD §3.6 | state | 深度牌、棋盘 | `src/game/MineBoardModel.ts`, `src/game/MineRowGenerator.ts` | 仅当四方向可达坑道连通到底部内侧列时上卷，初始底部边缘 EMPTY 不触发；新行由 `MineRowGenerator.generate` 生成 | 形成真实底部通路后深度牌数值提升 | ✅ |
| R11 | 稀有矿首次提示 | AC037/AC062 | state/toast | Toast 提示层 | `src/game/EndlessMineGame.ts` | 首次进入 biome 3 时写入 `rareOreUnlockedShown=true` 并 toast，之后不重复 | 首达浅海显示一次稀有矿提示 | ✅ |
| R12 | 音频/BGM/SFX 注册与播放 | PRD F051 / AC051/AC066 | static/animation | 程序化 Web Audio | `src/game/AudioGenerator.ts`, `src/game/EndlessMineGame.ts` | 注册 BGM、挖掘、破坏、炸弹、奖励、深度提升入口；动作流程调用且不改玩法数据 | 合法点击、破坏、奖励、推进有可调用音频反馈 | ✅ |
| R13 | standalone 验收映射 | AC001/AC002/AC004/AC005/AC009 | static | 等价映射常量 | `src/data/legacySceneMapping.ts` | `grid_logic/freeform`、`LevelManager.LEVEL_ORDER`、旧场景名和 `1152 x 768` 统一映射到竖屏 fixed-stage 实现；AC009 保留注册的 `UIScene`、`PauseUIScene`、`VictoryUIScene`、`GameCompleteUIScene` 均有 standalone 等价入口 | reviewer 可直接检查映射常量 | ✅ |
| R6 | 深度区背景切换 | PRD §3.7 | state/static | 页面背景 | `src/pages/RunPage/index.tsx` | `currentBiomeIndex` 映射五个矿区背景 | 深度区变化时背景切换 | ✅ |
| R7 | 掉落、收益和补给 | PRD §3.8 | state | 顶部收益栏、补给条 | `src/data/rewardConfig.ts`, `src/game/EndlessMineGame.ts` | 奖励写回 loot/tool/supply，满进度发放当前 biome 补给 | 顶部金币/矿石/材料和补给进度变化 | ✅ |
| R8 | HUD 信息展示 | PRD §3.9 | static/state | 顶部状态栏、收益栏、深度补给 | `src/pages/RunPage/index.tsx` | 读取 Redux `mineRun` 字段展示深度类型、推进、状态、收益 | 所有数值绑定 state | ✅ |
| R9 | 主动返场结算 | PRD §3.1/§3.9 | navigation/api | 收队返场按钮 | `src/hooks/useAppActions.ts`, `src/game/EndlessMineGame.ts` | `requestReturnToCamp` 生成 snapshot 并提交结算，延迟切到 SettlementPage | 点击后进入结算页 | ✅ |
| R10 | 工具耗尽自动结算 | PRD §3.9 / AC048/AC050/AC077 | state/navigation | 棋盘点击 | `src/game/EndlessMineGame.ts`, `src/hooks/useAppActions.ts` | 同一次动作先在引擎内合成 patch+loot 后的最终态，再通过 `onSettle('exhausted', finalSnapshot)` 写入结算，避免读取上一帧 React 闭包 | 工具耗尽后进入结算页并显示最终深度/矿区/收益 snapshot | ✅ |

## SettlementPage / settlement

| # | 功能点 | 来源 | 类型 | 触发/展示元素 | 实现位置 | 关键逻辑 | 可观察结果 | 自检 |
|---|---|---|---|---|---|---|---|---|
| S1 | 展示本局结算数据 | PRD §3.9 | static/state | 结算结果面板 | `src/pages/SettlementPage/index.tsx` | 读取 `finalSnapshot`，展示深度、深度类型、金币、宝石、矿石、宝箱 | 结算页显示真实 snapshot | ✅ |
| S2 | 再来一局 | 技术方案页面手册 | navigation/api | 再来一局按钮 | `src/pages/SettlementPage/index.tsx` | 调用 `replay -> startNewRun` | 点击重新进入 RunPage | ✅ |
| S3 | 返回标题 | 技术方案页面手册 | navigation | 返回标题按钮 | `src/pages/SettlementPage/index.tsx` | `goTitle -> setRoute(title)` | 点击返回 TitlePage | ✅ |
| S4 | 图片 SSOT 渲染 | 元素清单 S01/S03/S05/S06 | static | 背景、面板、按钮 | `src/pages/SettlementPage/index.750.less` | 按清单 bbox 渲染结果面板与两个按钮，标题并入背景不单独渲染 | 竖屏结算页单屏显示 | ✅ |
