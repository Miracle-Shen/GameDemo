# 《无限矿井》无尽模式 需求验收用例

> 来源 PRD: `/Users/miracleshen/GameAgent/projects/generic_endless_mine_endless_mode/game-agent/需求-最终PRD.md`  
> 生成日期: 2026-06-21  
> 用途: 供 coding 自检与独立评审逐条对照代码判定。每条用例均以可观察行为断言表达。

---

## 1. 覆盖摘要

- 功能总数: 52
- 覆盖功能数: 52 / 52
- 用例总数: 78
- 反向用例数: 14
- 状态流转用例数: 12

---

## 2. 功能编号映射

| 功能编号 | PRD 功能清单条目 |
|---|---|
| F001 | `grid_logic` + `freeform` 模板接入 |
| F002 | `1152 x 768` 分辨率配置 |
| F003 | `7 x 6` 可视棋盘 |
| F004 | `Preloader -> TitleScreen -> EndlessMineRunScene -> GameOverUIScene` 场景流转 |
| F005 | `LevelManager.LEVEL_ORDER = ['EndlessMineRunScene']` |
| F006 | 标题替换为“无限矿井” |
| F007 | 入口按钮文案改为“进入无尽矿井” |
| F008 | `EndlessMineRunScene` 继承 `BaseGridScene` |
| F009 | `UIScene`、`PauseUIScene`、`VictoryUIScene`、`GameCompleteUIScene`、`GameOverUIScene` 保留注册 |
| F010 | 不创建玩家可移动实体 |
| F011 | 不创建敌人实体 |
| F012 | 不创建移动收集物实体 |
| F013 | 可选宝箱、矿石提示、工具提示轻量展示实体 |
| F014 | Terrain layer 编码: EMPTY、WALL、FLOOR、SPECIAL |
| F015 | SPECIAL metadata: 遮挡层、内容类型、耐久、矿石提示、工具提示、掉落表编号 |
| F016 | 固定首屏 Terrain layer |
| F017 | 固定首屏 metadata layer |
| F018 | 初始木镐 `20`、横排炸弹 `2`、九宫格炸弹 `2` |
| F019 | 默认工具 `pick` |
| F020 | 木镐点击合法格消耗 `1` 木镐并造成 `1` 伤害 |
| F021 | 横排炸弹消耗 `1` 并命中当前行全部可破坏格 |
| F022 | 九宫格炸弹消耗 `1` 并命中目标 `3x3` 全部可破坏格 |
| F023 | 点击 WALL 不消耗工具并播放无效反馈 |
| F024 | 炸弹不破坏 WALL |
| F025 | 木镐只能点击与当前坑道连通的格 |
| F026 | 连通判定采用四方向邻接 |
| F027 | 遮挡层必须先破坏，不能越层伤害真实内容 |
| F028 | 炸弹可波及未连通但可见格，但施放落点必须是已连通的 `EMPTY` 坑道格 |
| F029 | 最底行与其他行同规则，不是预览层 |
| F030 | 每推进一层，棋盘上卷 1 行并生成新底行 |
| F031 | 新行至少保留 1 条从当前底部继续向下的可破坏路径 |
| F032 | 深度区系统: 地表矿区、深地表、浅海、深海、地心火山泥 |
| F033 | 按深度切换背景资源 |
| F034 | 稀有矿 biome 3 解锁 |
| F035 | 宝箱 biome 2 解锁 |
| F036 | 盲盒 biome 3 解锁 |
| F037 | 首次稀有矿 toast |
| F038 | sand、ore、blindBox、chest、tool、emptyReveal 收益规则 |
| F039 | 每推进 1 层 `supplyPoints +1` |
| F040 | 随机积分掉落范围 `1-4` |
| F041 | `supplyPoints >= supplyTarget` 时发放当前深度区工具补给 |
| F042 | 基础补给目标 `12`，每深度区 `+4` |
| F043 | 顶部状态行展示当前深度类型、本局推进、本局状态 |
| F044 | 顶部收益行展示金币、矿石、材料 |
| F045 | 底部工具区展示镐子、横炸、九炸、背包、收队返场 |
| F046 | 无通关条件 |
| F047 | 主动返场结算退出 |
| F048 | 工具耗尽自动结算退出 |
| F049 | `GameOverUIScene` 改为“本局收获结算”语义 |
| F050 | 结算页展示最终深度、深度类型、金币、矿石、材料、本局开启宝箱次数 |
| F051 | 音画资源注册 |
| F052 | 验收要求集合 |

---

## 3. 断言式功能验收用例

### 3.1 正向功能用例

| 用例 ID | 类型 | 覆盖功能 | 前置条件 | 操作 | 断言 |
|---|---|---|---|---|---|
| AC001 | 正向 | F001 | 应用已启动并加载配置 | 进入可玩场景 | 断言当前玩法使用 `grid_logic` 模板能力，且回合配置为 `freeform`，不会按固定步数关卡结算。 |
| AC002 | 正向 | F002 | 应用初始化完成 | 读取渲染/游戏配置 | 断言画布或游戏配置中的运行分辨率为 `1152 x 768`。 |
| AC003 | 正向 | F003 | 进入 `EndlessMineRunScene` | 观察棋盘 | 断言可视棋盘为 7 列 x 6 行，共 42 个可交互格位。 |
| AC004 | 正向 | F004 | 从应用冷启动开始 | 完成预加载，点击标题页入口，再结束本局 | 断言场景顺序可观察为 `Preloader -> TitleScreen -> EndlessMineRunScene -> GameOverUIScene`。 |
| AC005 | 正向 | F005 | 代码或运行配置可检查 | 读取 `LevelManager.LEVEL_ORDER` | 断言 `LEVEL_ORDER` 仅包含 `['EndlessMineRunScene']` 且首个入口关卡为 `EndlessMineRunScene`。 |
| AC006 | 正向 | F006 | 进入标题页 | 观察标题文字 | 断言标题页主标题展示“无限矿井”。 |
| AC007 | 正向 | F007 | 进入标题页 | 观察入口按钮 | 断言入口按钮文案展示“进入无尽矿井”。 |
| AC008 | 正向 | F008 | `EndlessMineRunScene` 已注册 | 检查运行时场景实例或源码继承关系 | 断言 `EndlessMineRunScene` 继承或等价复用 `BaseGridScene` 的棋盘生命周期能力。 |
| AC009 | 正向 | F009 | 应用初始化 | 检查场景注册表 | 断言 `UIScene`、`PauseUIScene`、`VictoryUIScene`、`GameCompleteUIScene`、`GameOverUIScene` 均保留注册。 |
| AC010 | 正向 | F010 | 进入可玩场景 | 检查实体列表和画面 | 断言不存在可方向移动的玩家实体，也不存在玩家移动控制表现。 |
| AC011 | 正向 | F011 | 进入可玩场景 | 检查实体列表和画面 | 断言不存在敌人实体、敌人行动阶段或敌人伤害表现。 |
| AC012 | 正向 | F012 | 进入可玩场景并获得奖励 | 观察奖励表现 | 断言奖励可以飞入 HUD 或直接入账，但不存在可在棋盘上移动的收集物实体。 |
| AC013 | 正向 | F013 | 首屏或生成行含宝箱/提示内容 | 观察对应格位 | 断言宝箱、矿石提示、工具提示如存在，仅作为轻量展示对象或 Scene 绘制对象，不要求移动实体行为。 |
| AC014 | 正向 | F014 | 棋盘初始化完成 | 检查 terrain 数据 | 断言 terrain layer 使用 `EMPTY=0`、`WALL=1`、`FLOOR=2`、`SPECIAL=6` 表达格子类型。 |
| AC015 | 正向 | F015 | 棋盘初始化完成 | 检查任一 `SPECIAL` 格 metadata | 断言 metadata 至少包含 `coverVisible`、`contentType`、`durability`、`hasOreHint`、`hasToolHint`、`rewardTableId` 字段。 |
| AC016 | 正向 | F016 | 开始新局 | 观察首屏 terrain | 断言首屏 terrain 与 PRD 固定 7x6 布局一致，包含空洞、墙和可破坏内容格。 |
| AC017 | 正向 | F017 | 开始新局 | 观察首屏 metadata | 断言首屏 metadata 与 PRD 固定 7x6 布局一致，包含 `S/O/B/W/T/X/E` 对应内容。 |
| AC018 | 正向 | F018 | 开始新局并完成初始化 | 观察工具栏 | 断言初始工具数量为镐子 `20`、横炸 `2`、九炸 `2`。 |
| AC019 | 正向 | F019 | 开始新局 | 观察当前选中工具 | 断言默认选中工具为 `pick`，且木镐按钮呈选中或默认可用状态。 |
| AC020 | 正向 | F020 | 当前工具为 `pick`，木镐数量大于 0，目标为合法可破坏格 | 点击目标格 | 断言木镐数量减少 1，目标格耐久减少 1，并触发挖掘反馈。 |
| AC021 | 正向 | F021 | 横排炸弹数量大于 0，选中横排炸弹，落点为已连通的 `EMPTY` 坑道格 | 点击该坑道格所在行 | 断言横排炸弹数量减少 1，当前行所有可破坏格均受到炸弹影响。 |
| AC022 | 正向 | F022 | 九宫格炸弹数量大于 0，选中九宫格炸弹，落点为已连通的 `EMPTY` 坑道格 | 点击该坑道格 | 断言九宫格炸弹数量减少 1，目标周围 `3x3` 范围内所有可破坏格均受到影响。 |
| AC023 | 正向 | F023 | 当前工具数量大于 0，目标为 `WALL` | 点击 `WALL` | 断言工具数量不变，目标墙格不变，并播放或展示无效反馈。 |
| AC024 | 正向 | F024 | 炸弹数量大于 0，炸弹影响范围内存在 `WALL` | 释放炸弹 | 断言范围内 `WALL` 仍保持不可破坏状态，编码与耐久不被炸弹改变。 |
| AC025 | 正向 | F025 | 当前工具为木镐，存在与坑道连通的可破坏格 | 点击该连通格 | 断言点击被判定为合法，并进入一次挖掘处理流水线。 |
| AC026 | 正向 | F026 | 存在只通过斜角相邻的目标格 | 检查连通判定 | 断言斜向相邻不构成连通，只有上下左右四方向邻接构成连通。 |
| AC027 | 正向 | F027 | 目标 `SPECIAL` 格 `coverVisible=true` 且真实内容未显露 | 点击目标格 | 断言本次伤害先破坏遮挡层，真实内容不会在同一次点击中被越层伤害结算。 |
| AC028 | 正向 | F028 | 炸弹落点是已连通的 `EMPTY` 坑道格，炸弹范围内含未连通但可见可破坏格 | 释放炸弹 | 断言未连通但可见可破坏格会被炸弹波及，且炸弹仅因落点位于可连通坑道上而释放成功。 |
| AC029 | 正向 | F029 | 最底行存在可破坏格、墙格和合法落点 | 点击或释放工具到最底行 | 断言最底行执行与其他可视行完全相同的点击、连通、墙体和炸弹规则。 |
| AC030 | 正向 | F030 | 底部形成真实可推进通路 | 完成一次行动处理 | 断言棋盘整体上卷 1 行，顶部删除 1 行，底部生成 1 行新数据，`currentDepth` 增加 1。 |
| AC031 | 正向 | F031 | 触发新行生成 | 观察新底行和后续合法路径 | 断言新行生成后至少存在 1 条从当前底部继续向下扩张的合法操作链。 |
| AC032 | 正向 | F032 | 将深度设置或推进到各阈值 | 观察 `currentBiomeName` | 断言深度 1-30 为地表矿区，31-80 为深地表，81-150 为浅海，151-260 为深海，261+ 为地心火山泥。 |
| AC033 | 正向 | F033 | 推进到不同深度区 | 观察背景资源 | 断言背景按深度区切换为 `mine_surface_bg`、`mine_mid_bg`、`mine_shallow_sea_bg`、`mine_deep_sea_bg`、`mine_core_bg`。 |
| AC034 | 正向 | F034 | 当前 biome 小于 3 与达到 3 分别生成内容 | 观察稀有矿相关内容 | 断言稀有矿在 biome 3 前不解锁，进入 biome 3 后允许出现或提示解锁。 |
| AC035 | 正向 | F035 | 当前 biome 小于 2 与达到 2 分别生成内容 | 观察宝箱内容 | 断言宝箱在 biome 2 前不作为新行生成内容出现，biome 2 及以后允许出现。 |
| AC036 | 正向 | F036 | 当前 biome 小于 3 与达到 3 分别生成内容 | 观察盲盒砖内容 | 断言盲盒砖在 biome 3 前不作为新行生成内容出现，biome 3 及以后允许出现。 |
| AC037 | 正向 | F037 | 首次进入 biome 3 且 `rareOreUnlockedShown=false` | 推进到浅海 | 断言首次稀有矿解锁 toast 展示一次，并将已展示标记置为 true。 |
| AC038 | 正向 | F038 | 依次挖开 sand、ore、blindBox、chest、tool、emptyReveal 内容 | 观察收益和工具变化 | 断言 sand 仅打通不直接给金币/材料，ore 给金币/矿石/材料组合，blindBox 给高波动组合，chest 自动开启并计数，tool 给工具，emptyReveal 仅显露坑道。 |
| AC039 | 正向 | F039 | 底部形成真实通路并推进 1 层 | 完成推进 | 断言 `supplyPoints` 增加 1。 |
| AC040 | 正向 | F040 | 触发随机积分掉落 | 观察积分入账 | 断言随机积分单次增加值在 `1-4` 闭区间内。 |
| AC041 | 正向 | F041 | `supplyPoints` 达到或超过 `supplyTarget` | 完成世界阶段结算 | 断言系统发放当前深度区对应工具补给，并刷新工具数量与补给条。 |
| AC042 | 正向 | F042 | 进入各深度区 | 观察 `supplyTarget` | 断言补给目标按深度区为 `12 / 16 / 20 / 24 / 28`。 |
| AC043 | 正向 | F043 | 进入运行场景并发生状态变化 | 观察顶部状态行 | 断言顶部状态行展示 `当前深度类型：{biomeName}`、`本局推进：{supplyPoints}/{supplyTarget}`、`本局状态：{runStatus}` 并随状态刷新。 |
| AC044 | 正向 | F044 | 进入运行场景并获得收益 | 观察顶部收益行 | 断言顶部收益行展示金币、矿石、材料，且收益入账后数值刷新。 |
| AC045 | 正向 | F045 | 进入运行场景 | 观察底部工具区 | 断言底部工具区展示 `[镐子 {woodPick}]`、`[横炸 {rowBomb}]`、`[九炸 {areaBomb}]`、`[背包]`、`[收队返场]`。 |
| AC046 | 正向 | F046 | 深度推进到任意深度 | 检查胜利条件 | 断言 `checkWinCondition()` 始终不触发胜利页或通关页。 |
| AC047 | 正向 | F047 | 单局运行中 | 点击 `[收队返场]` | 断言 `runStatus` 进入 `结算中`，并在结算延迟后进入 `GameOverUIScene`。 |
| AC048 | 正向 | F048 | 将三类工具都消耗或设置为 0 | 完成状态刷新 | 断言自动触发工具耗尽结算路径，并进入 `GameOverUIScene`。 |
| AC049 | 正向 | F049 | 进入 `GameOverUIScene` | 观察结算页文案 | 断言页面语义为“本局收获结算”，不展示失败作为主要语义。 |
| AC050 | 正向 | F050 | 进入结算页 | 观察结算数据 | 断言结算页展示最终深度、深度类型、金币、矿石、材料、本局开启宝箱次数。 |
| AC051 | 正向 | F051 | 应用预加载或进入相关场景 | 检查资源注册与播放 | 断言标题背景、五个矿区背景、矿块 tileset、HUD 工具图标、宝箱、矿石提示、工具提示、挖掘/破坏/炸弹/奖励/深度提升 SFX、BGM 均存在注册或等价可用引用。 |
| AC052 | 正向 | F052 | 完成一次可玩流程 | 对照验收要求集合 | 断言场景注册一致、基础配置保留、工具耗尽自动结算、主动返场结算、炸弹不破墙、木镐连通限制、炸弹落点必须在已连通 `EMPTY` 坑道格、最底行同规则、始终存在向下路径均可观察成立。 |

### 3.2 反向 / 边界用例

| 用例 ID | 类型 | 覆盖功能 | 前置条件 | 操作 | 断言 |
|---|---|---|---|---|---|
| AC053 | 反向 | F020,F025 | 当前工具为木镐，目标格不与任何坑道四方向连通 | 点击该目标格 | 断言本次点击无效，木镐数量不减少，目标格耐久不变化。 |
| AC054 | 反向 | F021,F028 | 横排炸弹数量为 0 | 选择横排炸弹并点击合法落点 | 断言炸弹不会释放，棋盘不发生炸弹命中变化。 |
| AC055 | 反向 | F022,F028 | 九宫格炸弹数量为 0 | 选择九宫格炸弹并点击合法落点 | 断言炸弹不会释放，棋盘不发生 `3x3` 命中变化。 |
| AC056 | 反向 | F028 | 炸弹数量大于 0，目标落点为坑道边缘可破坏格、未连通格、封闭格或 `WALL`，但不是已连通的 `EMPTY` 坑道格 | 点击该落点 | 断言炸弹施放失败，炸弹数量不减少，任何格子不受影响。 |
| AC057 | 反向 | F023,F024 | 当前工具为炸弹，爆炸范围覆盖多个 `WALL` | 释放炸弹 | 断言所有 `WALL` 均不被破坏，且不会产出奖励。 |
| AC058 | 反向 | F027 | 目标遮挡层仍可见，真实内容为 ore 或 chest | 单次点击目标格 | 断言本次只处理遮挡层，不会同时获得 ore/chest 奖励。 |
| AC059 | 反向 | F026 | 目标格仅与坑道斜向接触 | 使用木镐点击目标格 | 断言点击非法，斜向接触不会被当作连通。 |
| AC060 | 反向 | F029 | 最底行为 `WALL` 或非法未连通目标 | 点击最底行该格 | 断言最底行不因其位置特殊而放宽规则，不消耗工具或不破坏墙。 |
| AC061 | 反向 | F030,F031 | 底部未形成真实通路 | 完成一次合法挖掘但未打通底部 | 断言棋盘不上卷，不生成新底行，`currentDepth` 不增加。 |
| AC062 | 反向 | F034,F036,F037 | 当前 biome 为 1 或 2 | 生成新行多次 | 断言 biome 3 前不出现稀有矿解锁 toast，biome 3 前不生成盲盒砖。 |
| AC063 | 反向 | F035 | 当前 biome 为 1 | 生成新行多次 | 断言地表矿区新行不生成宝箱内容。 |
| AC064 | 反向 | F046 | 深度持续推进且收益增加 | 观察场景跳转 | 断言不会因为达到某个深度或收益阈值进入 `VictoryUIScene` 或 `GameCompleteUIScene`。 |
| AC065 | 反向 | F047,F048 | 已进入 `settlement_delay` 或 `GameOverUIScene` | 再次点击棋盘或工具按钮 | 断言不会继续消耗工具、推进深度或追加掉落。 |
| AC066 | 反向 | F051 | 某 SFX 播放时机触发 | 观察玩法数据 | 断言音效播放不改变工具、深度、收益等核心玩法数据。 |

### 3.3 状态流转用例

| 用例 ID | 类型 | 覆盖功能 | 前置条件 | 操作 | 断言 |
|---|---|---|---|---|---|
| AC067 | 状态流转 | F004,F006,F007 | 应用冷启动 | 等待预加载完成 | 断言状态从 `preloading` 进入 `title`，标题页展示“无限矿井”和“进入无尽矿井”。 |
| AC068 | 状态流转 | F004,F018,F019 | 位于标题页 | 点击“进入无尽矿井” | 断言进入 `run_initializing`，完成棋盘、工具、深度、补给、收益初始化后进入 `running_idle / 推进中`。 |
| AC069 | 状态流转 | F020,F025 | `running_idle / 推进中`，木镐目标合法 | 点击合法木镐目标 | 断言状态进入 `processing_action`，并调用一次点击处理流水线。 |
| AC070 | 状态流转 | F021,F022,F028 | `running_idle / 推进中`，炸弹落点是已连通 `EMPTY` 坑道格且对应库存大于 0 | 释放横排或九宫格炸弹 | 断言状态进入 `processing_action`，炸弹范围在处理流水线中结算。 |
| AC071 | 状态流转 | F023,F025,F028 | `running_idle / 推进中`，目标为 `WALL` 或非法目标 | 点击目标 | 断言状态保持 `running_idle / 推进中`，只播放无效反馈且不进入处理流水线。 |
| AC072 | 状态流转 | F038,F041 | `processing_action` 已完成破坏处理 | 等待 `onProcessComplete()` 完成 | 断言状态进入 `world_phase`，并在该阶段处理宝箱、资源、补给、toast。 |
| AC073 | 状态流转 | F030,F039 | `world_phase` 完成且底部形成真实通路 | 等待推进检测 | 断言状态进入 `advance_check` 后转入 `rolling_board`，并执行上卷、生成新行、`currentDepth +1`、`supplyPoints +1`。 |
| AC074 | 状态流转 | F030,F043 | `world_phase` 完成且底部未形成真实通路 | 等待推进检测 | 断言状态从 `advance_check` 进入 `state_refresh`，不会执行棋盘上卷。 |
| AC075 | 状态流转 | F030,F031 | `rolling_board` 后仍满足继续推进条件 | 等待链式处理 | 断言状态可继续保持 `rolling_board` 进行链式上卷，直到不再满足推进条件后进入 `state_refresh`。 |
| AC076 | 状态流转 | F043,F048 | `state_refresh` 时三类工具仍有任一大于 0 | 等待 HUD 刷新 | 断言 HUD 刷新后状态回到 `running_idle / 推进中`。 |
| AC077 | 状态流转 | F048,F049,F050 | `state_refresh` 时三类工具均为 0 | 等待结束检测与延迟 | 断言状态进入 `settling_exhausted / 工具耗尽`，再经 `settlement_delay 600ms` 进入 `GameOverUIScene`。 |
| AC078 | 状态流转 | F047,F049,F050 | `running_idle / 推进中` | 点击 `[收队返场]` | 断言状态进入 `settling_manual / 结算中`，再经 `settlement_delay 600ms` 进入 `GameOverUIScene`。 |

---

## 4. 自检结论

- 已从 PRD「功能清单」52 项逐项派生至少 1 条可观察断言。
- 已覆盖 PRD「核心状态机」中的启动、初始化、运行等待、动作处理、世界阶段、推进检测、棋盘上卷、状态刷新、主动结算、工具耗尽结算、结算页流转。
- 反向用例重点覆盖非法点击、库存不足、墙体不可破坏、遮挡层越层禁止、底部行不特殊、无通关条件、结算后输入冻结等边界。
